package com.napier.sem.models.enums;

//Holds information on which type of Object a generic Report needs to be cast to when generating CSV files
public enum ReportType {
    Country,
    City,
    CapitalCity,
    Population,
    Language,
    TotalPopulation
}
